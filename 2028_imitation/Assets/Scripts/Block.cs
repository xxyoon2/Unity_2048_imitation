using System.Collections;
using System.Collections.Generic;
using UnityEngine;

struct Block
{
    public bool checkBlanks;
    public int num;
    //Color 블럭 컬러;
}

public class 클래스이름 : MonoBehaviour
{
    private void Start()
    {
        // 블록 생성
        Block[,] blocks = new Block[4, 4];

        // 랜덤으로 2 블록 2개 생성하는 초기화 함수
    }

    // 2. 입력에 따라 블록 이동하고 합치는 함수 생성
    블록 이동()
    {

    }

}
